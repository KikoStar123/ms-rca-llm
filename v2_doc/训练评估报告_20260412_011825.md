# 轻量 SFT 训练与评估报告

- 模型：e:\Graduation_Project_online\models\Qwen2.5-7B-Instruct
- 数据划分：files:e:\Graduation_Project_online\output\train_split.jsonl
- 训练样本：1528
- 评估样本：200

## 本次微调配置
- 见同目录 `训练评估结果_20260412_011825.json` 中 `finetune` 字段（学习率、LoRA、epoch、是否跳过微调前等）。

## 微调前
- **已跳过**（`--skip-before-eval` / `SKIP_BEFORE_EVAL=1`）。同一基座、同一评估集上基线可复现，可直接对照历史 `llm_eval_before_*.jsonl`（如 `llm_eval_before_20260401_141203.jsonl`）。

## 微调后
- 可解析率：200/200
- 根因 Top‑1：0.645
- 根因 Top‑3：0.200
- 根因 Top‑5：0.365
- 故障类型 Acc：1.000
- KPI Acc：1.000
- 相关容器 Acc：1.000
- 完整匹配 Acc：0.645

## 备注
- 默认 `EVAL_OUTPUT_REPAIR=1`：`fault_type`/`kpi`/`related_container` 与输入 `fault_event` 对齐（符合题目约束，可抬高类型与完整匹配）。根因默认不改动；可选 `EVAL_ROOT_PROJECT_TO_CANDIDATES=1` 将根因投影到候选（部分数据金标不在候选时可能降分）。纯模型计分设 `EVAL_OUTPUT_REPAIR=0`。
- 详细预测见：llm_eval_before.jsonl / llm_eval_after.jsonl。
